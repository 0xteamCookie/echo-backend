# Loaded at startup via `import "dotenv/config"` in src/index.ts
DATABASE_URL="postgresql://user:password@localhost:5432/echo?schema=public"

# Firestore (Admin): project id required when using ADC without metadata (local dev).
FIREBASE_PROJECT_ID="echo-5beed"
# Path to Firebase service account JSON (from Firebase console → Project settings → Service accounts).
# GOOGLE_APPLICATION_CREDENTIALS="./your-project-firebase-adminsdk-xxxxx.json"
# FIRESTORE_EMULATOR_HOST="127.0.0.1:8080"
# Or paste JSON inline instead of a file:
# FIREBASE_SERVICE_ACCOUNT_JSON='{"type":"service_account",...}'

# Firestore: use emulator or service account (see README / firebase setup)
# GEMINI_API_KEY=   # required for AI triage on POST /api/data
GEMINI_MODEL="gemini-2.0-flash"
TRIAGE_ENABLED="true"
TRIAGE_NEARBY_RADIUS_M="500"

# CORS: `true` reflects request origin (dev). Set a single origin string in production.
# CORS_ORIGIN="true"
