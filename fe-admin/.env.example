# Base URL of the Echo backend (no trailing slash)
# Development default is http://localhost:3000
BACKEND_URL="http://localhost:3000"

# Must match ADMIN_API_KEY set in the backend .env
ADMIN_API_KEY="your-long-random-secret-here"
